

































































































































































































































































































































































































class King
  extends ChessPiece
{
  public King(ChessPlayer paramChessPlayer, ChessGame paramChessGame, ChessPosition paramChessPosition)
  {
    super(paramChessPlayer, paramChessGame, paramChessPosition);
    if (paramChessPlayer == paramChessGame.getPlayer1()) {
      mark = '♔';
    } else {
      mark = '♚';
    }
  }
  
  public void moveTo(ChessPosition paramChessPosition) throws IllegalMove {
    double d1 = getPosition().getX() - paramChessPosition.getX();
    double d2 = getPosition().getY() - paramChessPosition.getY();
    double d3 = Math.sqrt(d1 * d1 + d2 * d2);
    

    if ((d3 == 1.0D) || (d3 == Math.sqrt(2.0D))) {
      super.moveTo(paramChessPosition);
    } else {
      throw new IllegalMove(this, getPosition(), paramChessPosition);
    }
  }
}
